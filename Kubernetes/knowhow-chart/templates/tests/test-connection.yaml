apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "knowhow-chart.fullname" . }}-test-connection"
  labels:
    {{- include "knowhow-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "knowhow-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
