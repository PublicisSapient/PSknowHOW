apiVersion: v1
kind: Service
metadata:
  name: jira-processor
  namespace: {{ .Values.env}}-knowhow
  labels:
    {{- include "knowhow-jira-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knowhow-jira-processor.selectorLabels" . | nindent 4 }}
